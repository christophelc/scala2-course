object SparkMain {

  def main(args: Array[String]): Unit = {
	// write a module that allow you to:
	// - list files from a folder (provided as an argument)
	// - group files per size (0-10KB, 10KB-100KB, > 100KB)"
	// - create a report that gives a summary

	// functional spec ?
	// - do we have all the information we need ?

	// provide:
	//- technical spec (include error cases)
	//- unit tests
	//- integration tests
  }
}
